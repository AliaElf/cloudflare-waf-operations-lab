# macOS metadata
.DS_Store

# Original evidence stays private
Evidence/Raw/

# Exclude the Burp welcome-page demo because it displays example credentials
Evidence/Polished/09-burp-suite/45-burp-suite-community-installed.png

# Credentials and environment variables
.env
.env.*
*.pem
*.key

# Terraform local data and state (may contain sensitive values)
.terraform/
*.tfstate
*.tfstate.*
*.tfvars
*.tfvars.json
crash.log
crash.*.log
